import SwiftUI

struct HomeView: View {
    var body: some View {
        ScrollView {
            VStack {
                // Add content, such as videos, articles, etc.
            }
        }
        .navigationTitle("Home")
    }
}

struct HomeView_Previews: PreviewProvider {
    static var previews: some View {
        HomeView()
    }
}
