import SwiftUI

struct TeamView: View {
    var body: some View {
        NavigationView {
            ScrollView {
                VStack {
                    // Add teammates display
                }
            }
            .navigationTitle("Team")
        }
    }
}
